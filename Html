<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Executive AI Systems | You Lead. AI Executes.</title>
    <meta name="description" content="AI operating systems for executives who demand leverage — not productivity hacks.">
    <meta name="robots" content="index, follow">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter+Tight:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --matte-black: #0a0a0a;
            --deep-graphite: #1a1a1a;
            --electric-blue: #0077ff;
            --iridescent-blue: #00a8ff;
            --white: #ffffff;
            --light-gray: #f5f5f5;
            --dark-gray: #333333;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        html {
            scroll-behavior: smooth;
        }
        
        body {
            font-family: 'Inter Tight', sans-serif;
            background-color: var(--matte-black);
            color: var(--white);
            line-height: 1.6;
            overflow-x: hidden;
        }
        
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* Header & Navigation */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background-color: rgba(10, 10, 10, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            transition: all 0.3s ease;
        }
        
        header.scrolled {
            padding: 10px 0;
        }
        
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
        }
        
        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--white);
            text-decoration: none;
        }
        
        .logo span {
            color: var(--electric-blue);
        }
        
        nav ul {
            display: flex;
            list-style: none;
        }
        
        nav ul li {
            margin-left: 30px;
        }
        
        nav ul li a {
            color: var(--white);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }
        
        nav ul li a:hover {
            color: var(--electric-blue);
        }
        
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            color: var(--white);
            font-size: 1.5rem;
            cursor: pointer;
        }
        
        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            background: linear-gradient(135deg, var(--matte-black) 0%, var(--deep-graphite) 100%);
        }
        
        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 30% 50%, rgba(0, 119, 255, 0.1) 0%, transparent 50%);
            z-index: 1;
        }
        
        .hero-content {
            position: relative;
            z-index: 2;
            max-width: 700px;
        }
        
        .hero h1 {
            font-size: 4rem;
            font-weight: 700;
            line-height: 1.1;
            margin-bottom: 20px;
            background: linear-gradient(to right, var(--white) 0%, var(--electric-blue) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .hero p {
            font-size: 1.25rem;
            margin-bottom: 40px;
            color: rgba(255, 255, 255, 0.8);
        }
        
        .cta-buttons {
            display: flex;
            gap: 20px;
        }
        
        .btn {
            display: inline-block;
            padding: 15px 30px;
            border-radius: 5px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            cursor: pointer;
            border: none;
            font-family: 'Inter Tight', sans-serif;
            font-size: 1rem;
        }
        
        .btn-primary {
            background-color: var(--electric-blue);
            color: var(--white);
        }
        
        .btn-primary:hover {
            background-color: var(--iridescent-blue);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 119, 255, 0.3);
        }
        
        .btn-secondary {
            background-color: transparent;
            color: var(--white);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .btn-secondary:hover {
            border-color: var(--electric-blue);
            color: var(--electric-blue);
            transform: translateY(-3px);
        }
        
        .hero-visual {
            position: absolute;
            right: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 50%;
            height: 70%;
            z-index: 1;
            overflow: hidden;
        }
        
        .ai-portrait {
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--deep-graphite) 0%, var(--matte-black) 100%);
            border-radius: 10px 0 0 10px;
            position: relative;
            overflow: hidden;
        }
        
        .ai-portrait::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 70% 50%, rgba(0, 119, 255, 0.2) 0%, transparent 70%);
            animation: pulse 4s infinite alternate;
        }
        
        .data-streams {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0.5;
        }
        
        .stream {
            position: absolute;
            width: 2px;
            height: 100%;
            background: linear-gradient(to bottom, transparent, var(--electric-blue), transparent);
            animation: streamFlow 3s linear infinite;
        }
        
        @keyframes streamFlow {
            0% {
                transform: translateY(-100%);
            }
            100% {
                transform: translateY(100%);
            }
        }
        
        @keyframes pulse {
            0% {
                opacity: 0.3;
            }
            100% {
                opacity: 0.7;
            }
        }
        
        /* Sections */
        section {
            padding: 100px 0;
            position: relative;
        }
        
        .section-headline {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 30px;
            text-align: center;
        }
        
        .section-subhead {
            font-size: 1.25rem;
            color: rgba(255, 255, 255, 0.8);
            text-align: center;
            max-width: 800px;
            margin: 0 auto 50px;
        }
        
        .section-dark {
            background-color: var(--deep-graphite);
        }
        
        .promise-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 40px;
            align-items: center;
        }
        
        .promise-content {
            padding-right: 40px;
        }
        
        .promise-visual {
            background: linear-gradient(135deg, var(--matte-black) 0%, var(--deep-graphite) 100%);
            height: 400px;
            border-radius: 10px;
            position: relative;
            overflow: hidden;
        }
        
        .promise-visual::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 30% 70%, rgba(0, 119, 255, 0.2) 0%, transparent 70%);
        }
        
        .offer-card {
            background: linear-gradient(135deg, var(--deep-graphite) 0%, var(--matte-black) 100%);
            border-radius: 10px;
            padding: 50px;
            max-width: 900px;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
        
        .offer-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at 80% 20%, rgba(0, 119, 255, 0.1) 0%, transparent 70%);
        }
        
        .offer-headline {
            font-size: 2rem;
            margin-bottom: 20px;
        }
        
        .offer-tagline {
            font-size: 1.25rem;
            color: var(--electric-blue);
            margin-bottom: 30px;
        }
        
        .offer-features {
            margin: 40px 0;
        }
        
        .feature-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 20px;
        }
        
        .feature-icon {
            color: var(--electric-blue);
            margin-right: 15px;
            font-size: 1.25rem;
        }
        
        .demo-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 30px;
            margin-top: 50px;
        }
        
        .demo-card {
            background: linear-gradient(135deg, var(--deep-graphite) 0%, var(--matte-black) 100%);
            border-radius: 10px;
            padding: 30px;
            text-align: center;
            transition: transform 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
        
        .demo-card:hover {
            transform: translateY(-10px);
        }
        
        .demo-icon {
            font-size: 3rem;
            color: var(--electric-blue);
            margin-bottom: 20px;
        }
        
        .demo-title {
            font-size: 1.25rem;
            margin-bottom: 15px;
        }
        
        .positioning-content {
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
        }
        
        /* Footer */
        footer {
            background-color: var(--deep-graphite);
            padding: 60px 0 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }
        
        .footer-content {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 40px;
            margin-bottom: 40px;
        }
        
        .footer-column h3 {
            font-size: 1.25rem;
            margin-bottom: 20px;
            color: var(--electric-blue);
        }
        
        .footer-links {
            list-style: none;
        }
        
        .footer-links li {
            margin-bottom: 10px;
        }
        
        .footer-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: color 0.3s ease;
        }
        
        .footer-links a:hover {
            color: var(--electric-blue);
        }
        
        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            color: rgba(255, 255, 255, 0.5);
        }
        
        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            z-index: 2000;
            align-items: center;
            justify-content: center;
        }
        
        .modal-content {
            background: linear-gradient(135deg, var(--deep-graphite) 0%, var(--matte-black) 100%);
            width: 90%;
            max-width: 600px;
            border-radius: 10px;
            padding: 40px;
            position: relative;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .close-modal {
            position: absolute;
            top: 20px;
            right: 20px;
            background: none;
            border: none;
            color: var(--white);
            font-size: 1.5rem;
            cursor: pointer;
        }
        
        .modal h2 {
            margin-bottom: 20px;
            color: var(--electric-blue);
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
        }
        
        .form-group input,
        .form-group select {
            width: 100%;
            padding: 12px 15px;
            border-radius: 5px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            background-color: rgba(255, 255, 255, 0.05);
            color: var(--white);
            font-family: 'Inter Tight', sans-serif;
        }
        
        .payment-options {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin: 20px 0;
        }
        
        .payment-option {
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 5px;
            padding: 15px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .payment-option.selected {
            border-color: var(--electric-blue);
            background-color: rgba(0, 119, 255, 0.1);
        }
        
        .payment-icon {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }
        
        /* Security Badge */
        .security-badge {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background-color: var(--deep-graphite);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 5px;
            padding: 10px 15px;
            font-size: 0.8rem;
            display: flex;
            align-items: center;
            z-index: 100;
        }
        
        .security-icon {
            color: var(--electric-blue);
            margin-right: 8px;
        }
        
        /* Responsive */
        @media (max-width: 992px) {
            .hero h1 {
                font-size: 3rem;
            }
            
            .promise-grid {
                grid-template-columns: 1fr;
            }
            
            .promise-content {
                padding-right: 0;
                margin-bottom: 40px;
            }
            
            .demo-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .footer-content {
                grid-template-columns: repeat(2, 1fr);
            }
        }
        
        @media (max-width: 768px) {
            .header-container {
                padding: 15px 0;
            }
            
            nav ul {
                display: none;
            }
            
            .mobile-menu-btn {
                display: block;
            }
            
            .hero h1 {
                font-size: 2.5rem;
            }
            
            .hero-visual {
                display: none;
            }
            
            .cta-buttons {
                flex-direction: column;
            }
            
            .demo-grid {
                grid-template-columns: 1fr;
            }
            
            .footer-content {
                grid-template-columns: 1fr;
            }
            
            .payment-options {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Security Monitoring -->
    <div class="security-badge">
        <span class="security-icon">🔒</span>
        <span>Secure Connection</span>
    </div>

    <!-- Header -->
    <header id="header">
        <div class="container header-container">
            <a href="#" class="logo">Executive<span>AI</span></a>
            <nav>
                <ul>
                    <li><a href="#hero">Home</a></li>
                    <li><a href="#promise">Promise</a></li>
                    <li><a href="#offer">Upgrade</a></li>
                    <li><a href="#demo">Intel</a></li>
                    <li><a href="#positioning">About</a></li>
                    <li><a href="#" id="book-now-nav">Book Now</a></li>
                </ul>
            </nav>
            <button class="mobile-menu-btn" id="mobile-menu">☰</button>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="hero">
        <div class="container">
            <div class="hero-content">
                <h1>You Lead. AI Executes.</h1>
                <p>We build AI operating systems for executives who demand leverage — not productivity hacks.</p>
                <div class="cta-buttons">
                    <button class="btn btn-primary" id="book-call">Book Your AI Upgrade Call</button>
                    <a href="#demo" class="btn btn-secondary">See the System in Action</a>
                </div>
            </div>
        </div>
        <div class="hero-visual">
            <div class="ai-portrait">
                <div class="data-streams" id="data-streams"></div>
            </div>
        </div>
    </section>

    <!-- Section 1: The Core Promise -->
    <section class="section-dark" id="promise">
        <div class="container">
            <h2 class="section-headline">No theory. No overwhelm.</h2>
            <p class="section-subhead">Just systems that think like your best strategist — available 24/7.</p>
            
            <div class="promise-grid">
                <div class="promise-content">
                    <p>We design and install AI Chief of Staff systems that learn your leadership style, anticipate your decisions, and execute at scale.</p>
                    <p>This isn't automation. It's intelligence — built to expand your capacity, not replace it.</p>
                </div>
                <div class="promise-visual">
                    <!-- Abstract AI visualization -->
                </div>
            </div>
        </div>
    </section>

    <!-- Section 2: The Flagship Offer -->
    <section id="offer">
        <div class="container">
            <h2 class="section-headline">The Executive AI Upgrade</h2>
            <p class="section-subhead">A 90-day private implementation partnership that installs an AI-driven operating system into your leadership workflow.</p>
            
            <div class="offer-card">
                <h3 class="offer-headline">Transform Your Leadership Capacity</h3>
                <p class="offer-tagline">You bring the vision. We build the infrastructure.</p>
                
                <p>Integrating strategy, execution, and insight into a single intelligent command center.</p>
                
                <div class="offer-features">
                    <div class="feature-item">
                        <span class="feature-icon">✓</span>
                        <div>
                            <h4>Strategic AI Integration</h4>
                            <p>Custom AI systems aligned with your leadership style and business objectives.</p>
                        </div>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">✓</span>
                        <div>
                            <h4>Decision Intelligence</h4>
                            <p>AI-powered insights that anticipate challenges and identify opportunities.</p>
                        </div>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">✓</span>
                        <div>
                            <h4>Execution at Scale</h4>
                            <p>Automated workflows that free you to focus on high-impact strategic decisions.</p>
                        </div>
                    </div>
                </div>
                
                <p>By the end of 90 days, you'll lead with more clarity, bandwidth, and precision than ever before.</p>
                
                <button class="btn btn-primary" id="book-upgrade">Book Your Upgrade Call</button>
            </div>
        </div>
    </section>

    <!-- Section 3: Demonstrations (Framed as Intel) -->
    <section class="section-dark" id="demo">
        <div class="container">
            <h2 class="section-headline">Intelligence. Not Tutorials.</h2>
            <p class="section-subhead">Explore live workflow dashboards, strategic prompt libraries, and time-saving automations that give you back hours — all designed for executive decision-making.</p>
            
            <div class="demo-grid">
                <div class="demo-card">
                    <div class="demo-icon">📊</div>
                    <h3 class="demo-title">Executive Dashboard</h3>
                    <p>Real-time visualization of key metrics and strategic insights.</p>
                </div>
                <div class="demo-card">
                    <div class="demo-icon">🤖</div>
                    <h3 class="demo-title">Strategic Prompt Library</h3>
                    <p>Curated prompts for decision-making, analysis, and strategic planning.</p>
                </div>
                <div class="demo-card">
                    <div class="demo-icon">⚡</div>
                    <h3 class="demo-title">Workflow Automation</h3>
                    <p>Intelligent systems that handle routine tasks and complex workflows.</p>
                </div>
            </div>
            
            <div style="text-align: center; margin-top: 50px;">
                <a href="#" class="btn btn-secondary">Access the Intel Vault</a>
            </div>
        </div>
    </section>

    <!-- Section 4: Positioning Statement -->
    <section id="positioning">
        <div class="container">
            <div class="positioning-content">
                <h2 class="section-headline">Meet the AI Chief of Staff</h2>
                <p>You lead. AI executes.</p>
                <p>We partner with high-performance executives to install AI systems that think, plan, and act like a Chief of Staff — amplifying your leadership, not diluting it.</p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-column">
                    <h3>ExecutiveAI</h3>
                    <p>Building intelligent operating systems for visionary leaders.</p>
                </div>
                <div class="footer-column">
                    <h3>Services</h3>
                    <ul class="footer-links">
                        <li><a href="#offer">Executive AI Upgrade</a></li>
                        <li><a href="#">AI Strategy Consulting</a></li>
                        <li><a href="#">Custom AI Implementation</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <h3>Resources</h3>
                    <ul class="footer-links">
                        <li><a href="#demo">Intel Vault</a></li>
                        <li><a href="#">Case Studies</a></li>
                        <li><a href="#">Executive Insights</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <h3>Contact</h3>
                    <ul class="footer-links">
                        <li><a href="mailto:contact@executiveai.com">contact@executiveai.com</a></li>
                        <li><a href="#" id="footer-book">Book a Call</a></li>
                        <li><a href="#">Privacy Policy</a></li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2023 ExecutiveAI. All rights reserved. Secure and encrypted.</p>
            </div>
        </div>
    </footer>

    <!-- Booking Modal -->
    <div class="modal" id="booking-modal">
        <div class="modal-content">
            <button class="close-modal" id="close-modal">×</button>
            <h2>Book Your AI Upgrade Call</h2>
            <p>Schedule a confidential consultation to discuss your Executive AI Upgrade.</p>
            
            <form id="booking-form">
                <div class="form-group">
                    <label for="name">Full Name</label>
                    <input type="text" id="name" name="name" required>
                </div>
                
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" id="email" name="email" required>
                </div>
                
                <div class="form-group">
                    <label for="company">Company</label>
                    <input type="text" id="company" name="company">
                </div>
                
                <div class="form-group">
                    <label for="role">Role</label>
                    <input type="text" id="role" name="role">
                </div>
                
                <div class="form-group">
                    <label for="preferred-date">Preferred Date</label>
                    <input type="date" id="preferred-date" name="preferred-date" required>
                </div>
                
                <div class="form-group">
                    <label for="preferred-time">Preferred Time</label>
                    <select id="preferred-time" name="preferred-time" required>
                        <option value="">Select a time</option>
                        <option value="09:00">9:00 AM</option>
                        <option value="10:00">10:00 AM</option>
                        <option value="11:00">11:00 AM</option>
                        <option value="13:00">1:00 PM</option>
                        <option value="14:00">2:00 PM</option>
                        <option value="15:00">3:00 PM</option>
                    </select>
                </div>
                
                <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 20px;">Schedule Call</button>
            </form>
        </div>
    </div>

    <!-- Payment Modal -->
    <div class="modal" id="payment-modal">
        <div class="modal-content">
            <button class="close-modal" id="close-payment">×</button>
            <h2>Complete Your Executive AI Upgrade</h2>
            <p>Select your preferred payment method to secure your 90-day implementation partnership.</p>
            
            <div class="payment-options">
                <div class="payment-option" data-method="credit-card">
                    <div class="payment-icon">💳</div>
                    <div>Credit Card</div>
                </div>
                <div class="payment-option" data-method="wire-transfer">
                    <div class="payment-icon">🏦</div>
                    <div>Wire Transfer</div>
                </div>
            </div>
            
            <div id="credit-card-form" style="display: none;">
                <form id="payment-form">
                    <div class="form-group">
                        <label for="card-number">Card Number</label>
                        <input type="text" id="card-number" name="card-number" placeholder="1234 5678 9012 3456" required>
                    </div>
                    
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                        <div class="form-group">
                            <label for="expiry-date">Expiry Date</label>
                            <input type="text" id="expiry-date" name="expiry-date" placeholder="MM/YY" required>
                        </div>
                        
                        <div class="form-group">
                            <label for="cvv">CVV</label>
                            <input type="text" id="cvv" name="cvv" placeholder="123" required>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="card-name">Name on Card</label>
                        <input type="text" id="card-name" name="card-name" required>
                    </div>
                    
                    <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 20px;">Complete Payment</button>
                </form>
            </div>
            
            <div id="wire-transfer-info" style="display: none;">
                <p>Please use the following details for your wire transfer:</p>
                <div style="background: rgba(255,255,255,0.05); padding: 15px; border-radius: 5px; margin: 15px 0;">
                    <p><strong>Bank:</strong> Global Executive Bank</p>
                    <p><strong>Account Name:</strong> ExecutiveAI Systems</p>
                    <p><strong>Account Number:</strong> 123456789</p>
                    <p><strong>Routing Number:</strong> 021000021</p>
                    <p><strong>SWIFT/BIC:</strong> GEBUS33</p>
                </div>
                <p>Once your transfer is complete, please email the confirmation to <strong>payments@executiveai.com</strong></p>
                <button class="btn btn-primary" style="width: 100%; margin-top: 20px;" id="confirm-wire">I've Sent the Wire Transfer</button>
            </div>
        </div>
    </div>

    <script>
        // Create data streams for hero section
        function createDataStreams() {
            const container = document.getElementById('data-streams');
            const streamCount = 15;
            
            for (let i = 0; i < streamCount; i++) {
                const stream = document.createElement('div');
                stream.className = 'stream';
                stream.style.left = `${Math.random() * 100}%`;
                stream.style.animationDelay = `${Math.random() * 3}s`;
                container.appendChild(stream);
            }
        }
        
        // Header scroll effect
        window.addEventListener('scroll', function() {
            const header = document.getElementById('header');
            if (window.scrollY > 50) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });
        
        // Mobile menu toggle
        document.getElementById('mobile-menu').addEventListener('click', function() {
            const nav = document.querySelector('nav ul');
            nav.style.display = nav.style.display === 'flex' ? 'none' : 'flex';
        });
        
        // Modal functionality
        const bookingModal = document.getElementById('booking-modal');
        const paymentModal = document.getElementById('payment-modal');
        const bookCallBtn = document.getElementById('book-call');
        const bookUpgradeBtn = document.getElementById('book-upgrade');
        const bookNowNav = document.getElementById('book-now-nav');
        const footerBook = document.getElementById('footer-book');
        const closeModal = document.getElementById('close-modal');
        const closePayment = document.getElementById('close-payment');
        
        // Open booking modal
        function openBookingModal() {
            bookingModal.style.display = 'flex';
        }
        
        bookCallBtn.addEventListener('click', openBookingModal);
        bookUpgradeBtn.addEventListener('click', openBookingModal);
        bookNowNav.addEventListener('click', openBookingModal);
        footerBook.addEventListener('click', openBookingModal);
        
        // Close modals
        closeModal.addEventListener('click', function() {
            bookingModal.style.display = 'none';
        });
        
        closePayment.addEventListener('click', function() {
            paymentModal.style.display = 'none';
        });
        
        // Close modal when clicking outside
        window.addEventListener('click', function(event) {
            if (event.target === bookingModal) {
                bookingModal.style.display = 'none';
            }
            if (event.target === paymentModal) {
                paymentModal.style.display = 'none';
            }
        });
        
        // Booking form submission
        document.getElementById('booking-form').addEventListener('submit', function(e) {
            e.preventDefault();
            // In a real implementation, you would send this data to your backend
            alert('Your consultation has been scheduled. We will contact you shortly to confirm.');
            bookingModal.style.display = 'none';
            
            // Show payment modal after a brief delay
            setTimeout(function() {
                paymentModal.style.display = 'flex';
            }, 1000);
        });
        
        // Payment method selection
        const paymentOptions = document.querySelectorAll('.payment-option');
        const creditCardForm = document.getElementById('credit-card-form');
        const wireTransferInfo = document.getElementById('wire-transfer-info');
        
        paymentOptions.forEach(option => {
            option.addEventListener('click', function() {
                // Remove selected class from all options
                paymentOptions.forEach(opt => opt.classList.remove('selected'));
                
                // Add selected class to clicked option
                this.classList.add('selected');
                
                // Show appropriate form
                const method = this.getAttribute('data-method');
                if (method === 'credit-card') {
                    creditCardForm.style.display = 'block';
                    wireTransferInfo.style.display = 'none';
                } else if (method === 'wire-transfer') {
                    creditCardForm.style.display = 'none';
                    wireTransferInfo.style.display = 'block';
                }
            });
        });
        
        // Payment form submission
        document.getElementById('payment-form').addEventListener('submit', function(e) {
            e.preventDefault();
            // In a real implementation, you would process the payment via Stripe or similar
            alert('Payment processed successfully! Welcome to your Executive AI Upgrade.');
            paymentModal.style.display = 'none';
        });
        
        // Wire transfer confirmation
        document.getElementById('confirm-wire').addEventListener('click', function() {
            alert('Thank you for your confirmation. We will notify you once we receive the wire transfer.');
            paymentModal.style.display = 'none';
        });
        
        // Initialize on page load
        document.addEventListener('DOMContentLoaded', function() {
            createDataStreams();
        });
    </script>
</body>
</html>
